server:
  port: 9020

spring:
  profiles:
    active: local
  mvc:
    view:
      prefix: /templates/
      suffix: .jsp

# apollo配置
#app:
#  id: learn-consumer-apollo
#apollo:
#  meta: http://127.0.0.1:8080
#  bootstrap:
#    enabled: true
#    eagerLoad:
#      enabled: true

# 调整 controller 包的 log 级别，为了后面演示在配置中心动态配置日志级别。
logging:
  level:
    com:
      gf:
        controller: debug

# 开启springBoot的debug(可以在控制台看到已生效的配置类)
#debug: true